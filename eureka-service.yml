
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    metadata-map:
      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}:tttt